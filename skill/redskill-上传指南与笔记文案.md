# RedSkill 上传材料（草稿）

*2026-07-05 准备，2026-07-09 因未过审重写为「代码全内置」版本（后发现超字数限制），又加了一版「zip 打包」*

## 一、三个版本

| | 路径 | 字数/大小 | 说明 |
|---|---|---|---|
| **版本 A：GitHub 链接版** | `skill\redskill-upload\kids-art-box-backup-v1-repo-link\SKILL.md` | 约 5977 字 | 第一次上传被拒的那版；克隆模板仓库 + 部署到 GitHub Pages 的完整流程 |
| **版本 B：代码全内置版** | `skill\redskill-upload\kids-art-box\SKILL.md` | 约 69222 字，**超限 7 倍** | 把 App 全部源码写进文档，塞不进 10000 字，**无法上传**，留着做记录 |
| **版本 C：zip 打包版** | `skill\redskill-upload\kids-art-box-zip-version\kids-art-box\`（`SKILL.md` + `app-source.zip`） | SKILL.md 仅 2895 字；zip 105KB | 说明文字单独放 SKILL.md，App 源码打包进 `app-source.zip` 一起上传，不含任何外部链接 |

**版本 C 是否可行取决于 RedSkill 的 zip 上传到底支不支持真实源码文件**——网上几篇 5、6 月的报道说内测版"仅支持 Markdown 和 TXT 格式，YAML 配置及 Python 脚本等复杂文件无法上传"，如果这条限制现在还成立，zip 里的 `.ts`/`.tsx`/`.json` 源码文件可能同样传不上去或者传上去了但 AI 助手拿不到；但这些文章比较旧，UI 也可能已经更新支持更灵活的打包方式了。**这个不确定性只能靠实际上传一次来验证**，无法从这边确认。

`app-source.zip` 已验证：解压后 `npm install` + `npm run build`（含 TypeScript 类型检查）一次通过，是真实可用的代码，不是占位内容。

三个版本按顺序尝试：**先试版本 C**（如果 zip 能正常携带源码，这是最干净的方案）；zip 不行的话退回**版本 A**（已知能塞进字数限制，只是不确定是不是当初被拒的真正原因）。

（内部研发用的分文件版本在 `skill\kids-art-box\`，带 `references/` 和 `evals/` 测试数据，那个不要上传。）

### ⚠️ 第一次提交未过审，原因不明

小红书没给具体拒绝理由。推测大概率是内容里反复出现 `github.com` 裸链接，加上有几段教 AI 做 GitHub OAuth 设备码授权、拿 token 的技术流程——这类"外链 + 账号自动化"的内容容易被审核判定为导流或高风险操作。但这只是推测，重新提交时这个猜测才算真正被验证。

如果版本 A、C 都试过依然不过审，说明问题不是外链/OAuth，建议直接联系小红书客服要具体拒绝理由，不要再靠猜测反复改版本。

上传步骤（按活动说明）：
1. 电脑端登录小红书创作者中心 → RedSkill 管理入口
2. 上传对应版本的 `kids-art-box` 文件夹（版本 C 记得连 `app-source.zip` 一起上传；版本 A 上传前把文件夹名改回 `kids-art-box`）
3. 发笔记时"添加组件"→ RedSkill → 选择该技能 → 勾选原创 → 发布

## 二、笔记文案草稿

**标题备选（20 字内）：**
- 孩子的画堆成山？一句话让 AI 给娃做个作品馆
- 我把儿子的涂鸦做成了 App，全家都能翻
- 别再拍照存相册了！给娃的画一个专属美术馆

**正文：**

> 家里娃的画越攒越多，扔了舍不得，堆着落灰。
>
> 我用 Claude 给儿子做了一个"画作记忆盒"App：
> 📷 画拍进去，自动记下时间和地点
> 🎤 让娃自己讲一段"这画的是什么"，录音也存进去（这个最戳我，童言童语才是最珍贵的）
> 🖼 画廊翻看、点心心收藏、把最爱挂上"晾衣绳画作墙"
> 🔒 照片录音全存在设备本地，不上传任何服务器
>
> 现在我把整个流程打包成了 RedSkill 技能。你不需要会写代码——复制口令粘给电脑上的 AI 助手（Claude Code / Codex 都行），告诉它孩子的名字，10 分钟从零到本地/线上都能用。
>
> 免费部署在你自己的网站托管账号上，数据完全归你。
>
> 获取方式：点下方技能卡片 → 去使用 → 复制口令
>
> #RedSkill大赏 #AI育儿 #儿童画 #ClaudeCode #亲子

**配图建议：**
1. 封面：iPad 实拍画廊界面（贴纸胶带风很出片）
2. 图2：画作墙（晾衣绳）截图
3. 图3：详情页（语音故事播放条）
4. 图4：一张"一句话指令 → 几分钟后的成品"对比图

## 三、发布前检查

- [x] 版本 B（代码全内置，69222 字）确认超限，无法使用，仅留档
- [x] 版本 C 的 `app-source.zip` 已验证：解压后 `npm install` + `npm run build` 一次通过
- [ ] **先试版本 C**：创作者中心上传界面能不能正常接收 `app-source.zip`？上传后 SKILL.md 里引用的这个 zip 文件，AI 助手安装技能时能不能实际拿到（而不只是文字说明能过审，用户装上后却缺文件）？
- [ ] 版本 C 不行就试版本 A（GitHub 链接版）
- [ ] 截图里用演示数据，别泄露 Jasper 的真实画作/地点（地点会显示城市）
- [ ] 确认创作者中心有 RedSkill 内测权限
- [ ] 笔记里说明需要电脑端 AI Agent 工具（管理预期，减少差评）
- [ ] 版本 A、C 都试过依然不过审的话，别再自行改版本，直接找客服要具体理由
